const creds = {

  //create a bunch (pool) of potential connections for multiple users
  connectionLimit: 10,
  host: 'localhost',
  user: 'root',
  password: 'root',
  database: 'db_roku',
  port: 8889
}

module.exports = creds;